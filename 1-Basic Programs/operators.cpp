#include <iostream>
using namespace std;

int main()
{

/*
    //comparison/relational operators:



    int a = 2;
    int b = 3;

    bool first = (a == b);  //equals to
    cout << first << endl;

    bool second = (a > b); //greater than
    cout << second << endl;

    bool third = (a < b); //less than
    cout << third << endl;

    bool fourth = (a >= b); //greater than or equals to
    cout << fourth << endl; 

    bool fifth = (a <= b); //less than equals to
    cout << fifth << endl;

    bool sixth = (a != b); //not equals to
    cout << sixth << endl;

*/

    //logical operators:

    //NOT operator:

    int a = 24;
    cout << !a << endl;

    int b = 0;
    cout << !b << endl;

}
